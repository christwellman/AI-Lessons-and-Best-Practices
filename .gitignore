# hash cache for enrich_and_simplify.py — regenerable, machine/session-specific
.cache/

# raw source files (intermediate artifacts, only keep processed/)
raw/

# never commit API keys
.env
*.key
secrets.json

__pycache__/
*.pyc

# local virtualenv
.venv/
